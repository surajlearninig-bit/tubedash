version: '3.8'

services:

    tubedash-app:
       build: ${IMAGE_NAME}:${BRANCH}
       restart: unless-stopped
       ports:
         - "${APP_PORT}:8000"
       depends_on:
         -  redis-cache
       environment:
         -  REDIS_HOST=redis-cache

    redis-cache:
       image: "redis:alpine"
       restart:  unless-stopped
       volumes:
         - ./redis_data:/data
